The `solver/` directory contains a program for calculating the optimal solution to every possible game of Stumper. You can read more about it [here](https://csun.io/solving-stumper).

The `stumper_unity/` directory is the Unity `6000.0.32f1` project for the game itself.
